#include "Sim.h"
#include <iostream>
#include <fstream>
#include <cstdlib>
#include <stdio.h>

using namespace std;

int main(int argc, char** argv)
{
    // GenTree<int> tree;
    // tree.insert(10);
    // tree.insert(4);
    // tree.insert(1);
    // tree.insert(5);
    // tree.insert(15);
    // tree.insert(12);
    // tree.insert(18);
    // cout << "MIN " << rand() % 100 << endl;
    // tree.printTree();
    // tree.deleter(1);
    // tree.printTree();
    // tree.find(10);

    Sim test;
    test.run();

    return 0;
}
